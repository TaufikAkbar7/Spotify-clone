import React from 'react'

const Title = ({ title }) => {
    return (
        <div>
            <span className="text-4xl text-white font-bold">{title}</span>
        </div>
    )
}

export default Title
